{
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "clean": "rm -rf build"
  },
  "devDependencies": {
    "@snowpack/plugin-webpack": "^2.3.1",
    "snowpack": "^3.2.2",
    "webpack-bundle-analyzer": "^4.4.0"
  },
  "dependencies": {
    "enum": "^3.0.4",
    "preact": "^10.5.13",
    "redaxios": "^0.4.1"
  }
}
