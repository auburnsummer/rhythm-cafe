/*
A function that does nothing. I'm just using this to alias "preact/debug" to in production so that I can
leave the "preact/debug" call in without having to put it in and out all the time.
*/

export default function noop() {
    return;
}